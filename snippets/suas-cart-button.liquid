{% unless request.page_type == 'password' %}
  <a class="suas-floating-cart" href="{{ routes.cart_url }}" aria-label="Open cart">
    <span class="suas-floating-cart__icon" aria-hidden="true">
      <svg viewBox="0 0 24 24" focusable="false">
        <path d="M7 8h13l-1.25 7.25a2 2 0 0 1-1.97 1.66H9.28a2 2 0 0 1-1.97-1.66L5.75 5.8H3" />
        <path d="M9.5 20.2h.01M17 20.2h.01" />
      </svg>
    </span>
    <span class="suas-floating-cart__label">cart</span>
    <span class="suas-floating-cart__count" data-suas-cart-count>{{ cart.item_count }}</span>
  </a>

  <style>
    .suas-floating-cart {
      position: fixed;
      top: 24px;
      left: 190px;
      right: auto;
      bottom: auto;
      z-index: 9999;
      display: inline-grid;
      place-items: center;
      width: 44px;
      height: 44px;
      border: 1px solid rgba(16, 16, 16, 0.14);
      border-radius: 999px;
      background: linear-gradient(180deg, #fff 0%, #ffd8e8 150%);
      color: #101010;
      box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.92), 0 8px 18px -11px rgba(16, 16, 16, 0.42);
      font-family: 'Casual Human', 'CasualHuman', Futura, sans-serif;
      line-height: 1;
      text-decoration: none;
      transition: transform 180ms ease, box-shadow 180ms ease, background 180ms ease;
    }

    .suas-floating-cart:hover,
    .suas-floating-cart:focus-visible {
      background: #101010;
      color: #fff;
      box-shadow: 0 5px 0 #f8cddd;
      transform: translateY(-2px);
      text-decoration: none;
      outline: none;
    }

    .suas-floating-cart__label {
      position: absolute;
      width: 1px;
      height: 1px;
      overflow: hidden;
      clip: rect(0 0 0 0);
      white-space: nowrap;
    }

    .suas-floating-cart__icon {
      display: inline-grid;
      place-items: center;
      width: 22px;
      height: 22px;
      flex: 0 0 auto;
    }

    .suas-floating-cart__icon svg {
      width: 22px;
      height: 22px;
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
    }

    .suas-floating-cart__count {
      position: absolute;
      top: -6px;
      right: -7px;
      display: inline-grid;
      place-items: center;
      min-width: 20px;
      height: 20px;
      padding: 0 5px;
      border: 1.5px solid #101010;
      border-radius: 999px;
      background: #d9ff78;
      color: #101010;
      font-family: Futura, Arial, sans-serif;
      font-size: 10px;
      font-weight: 900;
      line-height: 1;
    }

    @media (max-width: 1023px) {
      .suas-floating-cart {
        top: 45px;
        left: calc(50% + 49px);
        width: 34px;
        height: 34px;
        box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.92), 0 6px 14px -10px rgba(16, 16, 16, 0.5);
      }

      .suas-floating-cart__icon,
      .suas-floating-cart__icon svg {
        width: 18px;
        height: 18px;
      }

      .suas-floating-cart__count {
        top: -5px;
        right: -7px;
        min-width: 18px;
        height: 18px;
        padding: 0 4px;
        font-size: 9px;
      }
    }

    @media (max-width: 360px) {
      .suas-floating-cart {
        left: auto;
        right: 16px;
      }
    }
  </style>

  <script>
    (() => {
      const countNode = document.querySelector('[data-suas-cart-count]');
      if (!countNode) return;

      fetch('{{ routes.cart_url }}.js', { credentials: 'same-origin' })
        .then((response) => response.ok ? response.json() : null)
        .then((cart) => {
          if (cart && typeof cart.item_count === 'number') {
            countNode.textContent = cart.item_count;
          }
        })
        .catch(() => {});
    })();
  </script>
{% endunless %}
